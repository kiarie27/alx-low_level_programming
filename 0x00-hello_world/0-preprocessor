#!/bin/bash

# Check if the CFILE variable is set
if [ -z "$CFILE" ]; then
    echo "CFILE environment variable is not set. Please set it to the C file name."
    exit 1
fi

# Run the C preprocessor and save the result into the "c" file
gcc -E "$CFILE" -o c

echo "Preprocessed file saved as 'c'"
